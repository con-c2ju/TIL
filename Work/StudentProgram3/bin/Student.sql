CREATE TABLE STUDENT(
	NUM NUMBER(5) PRIMARY KEY,	-- 한자리가 1바이트
	NAME VARCHAR2(30) NOT NULL,
	KOR NUMBER(3),
	ENG NUMBER(3),
	MATH NUMBER(3)
);

CREATE SEQUENCE STUSEQ
INCREMENT BY 1
START WITH 100;

ALTER TABLE STUDENT
ADD ORDER BY NUM;

SELECT * FROM STUDENT;